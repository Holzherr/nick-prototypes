// Public Supabase config for the maths-garden project. The anon key is safe to ship; row-level
// security does the gating. Blank until the project exists (see README → Backend).
export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
