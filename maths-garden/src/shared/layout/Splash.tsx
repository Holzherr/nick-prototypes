import { AppIcon } from '@/shared/brand/AppIcon';

export const Splash = () => (
  <div className="relative z-10 flex min-h-dvh items-center justify-center">
    <AppIcon size={96} className="animate-pulse" />
  </div>
);
