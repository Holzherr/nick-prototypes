name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  check-css:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Check every class used in markup has a CSS rule
        run: node tools/check-css.js 'echo/*.html'

  deploy:
    needs: check-css
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: |
            qeued-next/package-lock.json
            maths-garden/package-lock.json

      # TigerWorkouts moved to Holzherr/tigerworkouts on 18 Sep 2026. workout-hub/ and
      # workout-hub-next/ are now static redirects, copied with everything else.

      # qeued-next is a Vite + Storybook project (base /nick-prototypes/qeued-next/).
      - name: Build qeued-next
        working-directory: qeued-next
        run: |
          npm ci
          npm test
          npm run build
          npm run build-storybook

      # maths-garden is the same shape again (base /nick-prototypes/maths-garden/).
      # check-types and lint were missing here: a type error or a lint regression could reach main and
      # only surface later at build time, or on a device.
      - name: Build maths-garden
        working-directory: maths-garden
        env:
          VITE_SUPABASE_URL: ${{ vars.MATHS_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ vars.MATHS_SUPABASE_ANON_KEY }}
        run: |
          npm ci
          npm run check-types
          npm run lint
          npm test
          npm run build
          npm run build-storybook

      - name: Assemble site
        run: |
          mkdir _site
          rsync -a --exclude .git --exclude _site --exclude node_modules --exclude qeued-next --exclude maths-garden ./ _site/
          cp -r qeued-next/dist _site/qeued-next
          cp -r maths-garden/dist _site/maths-garden

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
